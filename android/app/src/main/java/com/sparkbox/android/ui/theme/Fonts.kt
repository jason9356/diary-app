package com.sparkbox.android.ui.theme

import androidx.compose.ui.text.font.FontFamily

/**
 * System sans — uses the phone’s built-in CJK face (MiSans / OPPO Sans / Noto…).
 * Avoids Google Fonts download jank and large bundled TTF.
 */
val AppFontFamily: FontFamily = FontFamily.SansSerif

val DisplayFontFamily: FontFamily = AppFontFamily
