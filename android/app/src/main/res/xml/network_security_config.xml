<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug / LAN sync uses HTTP.
  Production VPS should use HTTPS; keep cleartext only for local testing.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
