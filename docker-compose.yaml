# Reserved for a future sync server. Local diary does not need Docker.
#
# To avoid accidental startup, the placeholder service uses profile "future".
#   docker compose --profile future up

services:
  sync-api-placeholder:
    profiles: ["future"]
    image: busybox:latest
    command: ["echo", "Sync API not implemented in v1"]
